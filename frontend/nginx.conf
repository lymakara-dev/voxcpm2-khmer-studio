server {
    listen 8080;
    root /usr/share/nginx/html;
    index index.html;

    location /api/ {
        proxy_pass http://backend:8000;
        proxy_read_timeout 300s;   # synthesis can take a while on first load
        proxy_send_timeout 300s;
    }

    location / {
        try_files $uri /index.html;
    }
}
